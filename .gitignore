node_modules/
dist/
build/
# …except the desktop package's, which holds the committed app icons. Without them
# electron-builder fails on a fresh checkout with "icon not found", which would only
# show up in CI.
!packages/desktop/build/
!packages/desktop/build/**
out/
release/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.local
.vite/
.turbo/
index.db
index.db-*
/data/
/packages/server/data/
/tmp/
migration-report.html
.tsc/
packages/desktop/release/
.smoke/

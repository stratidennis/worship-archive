# Where the .chopro library and index.db live. Absolute paths only — a relative path
# resolves against the server package, not the repo root.
WORSHIP_DATA=/absolute/path/to/worship-archive/data

# API port. The UI dev server runs on 7373 and proxies /api here.
PORT=7374

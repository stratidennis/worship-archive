# Generated by pnpm in its own format. Reformatting it achieves nothing: the next
# install rewrites it, and `format:check` fails again.
pnpm-lock.yaml

# Build output.
dist/
build/
release/
.tsc/
.smoke/
coverage/

# The song library. These are `.chopro` and data files, not source, and the migration
# report is generated HTML.
data/
migration-report.html

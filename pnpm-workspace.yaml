packages:
  - 'packages/*'
  - 'tools'

# pnpm blocks dependency install scripts by default. esbuild (used by Vite and
# Vitest) needs its postinstall to fetch the right platform binary.
allowBuilds:
  esbuild: true
